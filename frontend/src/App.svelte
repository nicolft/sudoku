<script lang="ts">
	export let name: string;


	async function createGame() {
		// Create a new game and store the returned id.
		let id = await fetch('/create').then( resp => {return resp.text()} );

		// TODO: Route to '/play' with game id headers
	}
</script>

<main>
	<h1>Hello {name}!</h1>
	<p>Visit the <a href="https://svelte.dev/tutorial">Svelte tutorial</a> to learn how to build Svelte apps.</p>

	<button on:click|once={createGame}>
		Create a Game
	</button>
</main>

<style>
	main {
		text-align: center;
		padding: 1em;
		max-width: 240px;
		margin: 0 auto;
	}

	h1 {
		color: #ff3e00;
		text-transform: uppercase;
		font-size: 4em;
		font-weight: 100;
	}

	@media (min-width: 640px) {
		main {
			max-width: none;
		}
	}
</style>